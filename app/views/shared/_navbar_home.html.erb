  <nav class="navbar navbar-dark navbar-expand-md navigation-clean-search w-100" id="navbarhome">
    <div class="container-fluid">
      <%= link_to root_path, class: "navbar-brand" do %>
        <i class="fas fa-dice-d20 fa-lg"></i>
          <% end %>
          RoboInvest
        <button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="navbar-toggler-icon"></span>
        </button>
      <div class="collapse navbar-collapse" id="navcol-1">
      <%= form_with(url: search_path,
          method: "get",
          html: {class: "form-inline d-none d-sm-inline-block mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search"}
          ) do |f| %>
          <%= f.text_field :query,
                class: "form-control rounded-pill",
                id: "search-bar",
                placeholder: "Find Stock"
              %>
              <%= f.submit "Stock", class: "btn btn-light action-button text-white" %>
          <% end %>
          <span class="navbar-text"><%= link_to "Login", new_user_session_path, class: "login" %></span><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-light action-button sign-up" %>
      </div>
    </div>
  </nav>
