<div class="w-100" style="min-height: 100vh">
  <div class="login-container d-flex flex-column align-items-center">
    <div class="form-container d-flex flex-column justify-content-center shadow mt-5 mr-5">
      <div class="d-flex justify-content-center ">
        <h2 class='login-heading mb-5'>New Wallet !</h2>
      </div>
          <%= simple_form_for [ @wallet ] do |f| %>
            <div class="form-inputs mx-4 mb-5">
              <%= f.input :name %>
              <%= f.input :photo, as: :file, input_html: { class: 'd-none' }, label_html: { class: 'upload-photo'}, label: '📷 Upload a photo' %>
            </div>
            <div class='d-flex justify-content-center'>
              <%= f.submit class: "btn btn-outline-secondary rounded-pill text-white mb-3 mt-5" %>
            </div>
          <% end %>
    </div>
  </div>
</div>
